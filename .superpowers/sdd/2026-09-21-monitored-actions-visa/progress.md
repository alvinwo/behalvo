# SDD ledger — plan: docs/superpowers/plans/2026-09-21-monitored-actions-visa.md

- Task 4: implementation complete on base `49894eb`; first independent-review fix committed as `e23903c`; second scoped fix commit: `fix: preserve monitored recovery evidence`.
- Pre-flight: Task 4 consumes the merged Task 1 journal/service-job storage and Task 2 runtime fence/operation-start contracts; inspected plan/spec require atomic grant reservation with `action.started`, recurring monitor jobs, and installation-generation barriers without changing legacy behavior.
- Delivered: generic journaled grants, exact digest/revision and binding validation, explicit activation/revocation/reconciliation, deterministic observation narrowing, one-effect atomic reservation/settlement, durable monitor jobs with budgets/jitter/backoff/pause/recovery, encrypted provenance validation, and an explicit legacy service-schema upgrade path.
- Boundary: no browser/native-host, credential, portal, visa, or external-effect implementation from Tasks 5–7 was added.
- Original implementation evidence: focused Task 4 gate 23 passed; full `npm test` 706 passed / 0 failed / 2 skipped; `npm run verify` passed all checks and three offline demos; `git diff --check` passed immediately before commit.
- Ruling: Task 4's required `monitor` service-job kind changes the exact SQLite CHECK and encrypted snapshot validator, so `sqlite-schema.ts` and `sqlite-validation.ts` were necessarily updated even though the plan's Task 4 file list named only `sqlite-store.ts` and `service-jobs.ts`; the prior exact v3/v4 schema remains readable and upgrades only when explicitly requested.
- Review resolution: verified all six findings in `task-4-review.md` against source and specification; all were valid. Fixed atomic grant-terminal monitor/job settlement, bounded non-cooperative inspection, authenticated restored-copy activation state, strictly positive jittered delays, evidence-checked monotonic settlement, and exact reservation/start replay provenance.
- Review evidence: focused Task 4 gate 44 passed; compatibility gate 170 passed; full `npm test` 727 passed / 0 failed / 2 skipped; fresh read-only review found no remaining issues; `npm run verify` passed all five steps with unchanged source fingerprint in `data/verification/2026-09-21T11-22-31.061Z-3659f4f9-d7d0-4b3d-a70d-152bce4bdd65/summary.json`; final `git diff --check` passed.
- Re-review resolution: verified the two remaining P2 findings against `e23903c`; both were valid. Typed local stop/deadline errors now bypass provider-failure normalization regardless of signal timing, and an exact later satisfied readback can monotonically consume the same retained `not_satisfied` reservation. Both the verification record and provider observation must be strictly later than the negative settlement; mismatched evidence remains rejected; duplicates are idempotent; capacity is never restored; historical non-monotonic verification records remain replayable.
- Re-review TDD: deterministic pre-abort deadline classification failed by completing `provider_unavailable` before the typed-stop guard; stale verification initially appended; and exact negative-to-positive settlement failed with `Invalid monitored grant settlement`. Each failed before its minimal production change and passed afterward.
- Re-review evidence: focused Task 4 gate 47 passed, including 80 deterministic deadline-order iterations and late resolve/reject fencing; compatibility gate 170 passed; full `npm test` 730 passed / 0 failed / 2 skipped; `npm run verify` passed all five steps with unchanged source fingerprint in `data/verification/2026-09-21T11-42-36.985Z-b2d9c720-905e-47b5-aab0-8e97a94c4aee/summary.json`; final diff check pending the evidence-only edits.
- Final review resolution: fresh read-only review accepted the deadline fix and found one Important settlement-evidence issue. The global timestamp guard was removed to preserve legacy replay; equal record time, older provider observation time, and legacy non-monotonic record time now have explicit RED/GREEN coverage at the negative-to-positive settlement boundary. No second review was dispatched after this TDD fix pass.
- Task 5: implementation complete on Task 4 base `673cc22`; delivered in the scoped `feat: add fenced local browser sessions` commit.
- Task 5 delivered: local-only one-host MV3 extension, exact 32 KiB typed protocol, independent native/background/content validation, exclusive random browser epochs, trusted-fence rechecks, durable human handoff and exact resume preflight, strict native framing, and the deterministic synthetic scheduling portal with crash/race durability coverage.
- Task 5 boundary: no Keychain, real credentials, live visa adapter, production extension installation, real portal account, or external network integration from Tasks 6–7 was added.
- Task 5 TDD: initial focused RED failed all five new test files because the browser surface did not exist; first complete GREEN was 14/14. Later RED/GREEN slices fixed exact extension forwarding/snapshots, calendar instant validation, fatal UTF-8 parsing, valid synthetic review state, non-settling handoff release, strict binding bounds, absolute native executable paths, exact durable envelopes, pending-pause click fencing, and service-shutdown invalidation.
- Task 5 evidence: focused and compatibility gate 100 passed / 0 failed / 0 skipped; full `npm test` 755 passed / 0 failed / 2 skipped; `npm run verify` passed all five steps with unchanged source fingerprint in `data/verification/2026-09-21T12-19-22.197Z-71e88cb6-4e4c-48d7-9c7e-941ab3cc78df/summary.json`; every demo reported zero real external effects; final diff check passed before the evidence-only edits.
- Ruling: Task 5 uses `tsconfig.extension.json` in addition to the plan's named root `tsconfig.json`, isolating browser globals and `extension/dist` from the existing Node package output while keeping root build/typecheck authoritative; the cost if later consolidated is one small build-config migration.
- Task 5 independent-review resolution: reproduced and accepted all seven findings in `task-5-review.md` before editing. The manifest content artifact is now a real classic script; the live native/background/content path has exact activation, monotonic replay state, two-phase gesture commit, fresh-document sequence binding, and acknowledged revocation; immutable operation tokens cannot revive after resume; failed/ambiguous pause is faulted until explicit recovery; and lifecycle transitions are serialized and post-await fenced.
- Task 5 portal/native resolution: authoritative HTML now represents every recognized schema and uses fixed same-origin page/select/exact slot-plus-intent submit/readback flows with no false slot. Native writes contain callback, destroy, close, timeout, EOF, delayed raw error, and concurrent-write failure paths behind fixed errors. No arbitrary script/network/cookie/credential surface or Task 6–7 integration was introduced.
- Task 5 review TDD: independent REDs reproduced classic parse failure, replay/foreign clicks, late post-handoff click, stale-epoch authorization after resume, fail-open pause, resume/shutdown and concurrent-resume races, false portal slots and invalid snapshots, and uncaught raw stream errors. Further audit REDs covered generic submit selection, duplicate revoke, revoke during document bind, revoke overtaking activation, stranded concurrent native exchange, and coerced intent values; all are GREEN.
- Task 5 review evidence: focused plus Task 4 compatibility gate 122 passed / 0 failed / 0 skipped; full `npm test` 777 passed / 0 failed / 2 skipped; `npm run verify` passed all five steps with unchanged source fingerprint in `data/verification/2026-09-21T13-22-36.811Z-e95109ec-e2eb-4005-a587-3eaf1f0f39fb/summary.json`; all demos reported zero real external effects; final post-report `git diff --check` passed.
- Updated ruling: `tsconfig.extension.json` emits the MV3 background ES module and `tsconfig.extension-content.json` emits the manifest's self-contained classic content artifact; root build/typecheck remains authoritative over both browser contexts and Node output.
- Task 5 final re-review resolution: reproduced all three remaining findings before edits. Trusted native-generated operation ID plus bounded TTL and explicit cancellation now reach background and the exact compiled document; content rechecks active binding, document, operation, cancellation, and local monotonic expiry immediately before mutation. Fresh documents acknowledge revoke without acquiring authority, and bounded retired bindings prevent delayed old binds from reviving an epoch. Native timeout/close bounds the entire exchange even when the writable callback never settles, with immediate rejection consumption and harmless late callbacks/errors.
- Task 5 final re-review TDD: REDs covered abort after authorization, expired and aborted framed commits, fresh-document revoke, delayed bind after revoke, stalled-write timeout/close process failure, and cancellation suspended during document bind. The 7 timing-sensitive regressions passed 10 consecutive iterations (70/70); focused plus Task 4 compatibility gate passed 129/129.
- Task 5 final re-review evidence: full `npm test` 784 passed / 0 failed / 2 skipped; `npm run verify` passed all five steps with unchanged source fingerprint `c51abf269fb0145e645faf24095d9308aec1d462295a73f0e570b8643753ab2c` in `data/verification/2026-09-21T13-57-20.440Z-fbbf9799-07e3-4c6a-9f0d-2f179b570bc4/summary.json`; every demo reported zero real external effects. Final diff check remains after evidence and review edits.
- Task 5 final round-3 resolution: reproduced the remaining P1 through real native framing, exported background, manifest-compiled content, and authoritative synthetic booking. One native-generated absolute monotonic expiry now survives every hop without TTL rebasing; background retains cancellable preparing/committing authority through content settlement; cancellation overtakes held delivery; content performs the exact final expiry/cancel/document/operation check; completed mutation and late cancellation linearize as `gesture.settled` without failing the channel or permitting retry. The installed background dispatcher suppresses only the superseded original completion and keeps the same port usable for acknowledged revoke/shutdown.
- Task 5 adjacent final-review resolution: reproduced delayed bind delivered to replacement document B after revoke on document A. A bounded background-authoritative retired-binding set, fresh-document retirement announcement, and compensating stale-bind revoke preserve fresh activation. Exact `document.bound` validation now rejects wrong protocol versions.
- Task 5 final round-3 evidence: the 5 new timing/replacement/version cases passed 10 repeated runs (50/50), and both installed-dispatcher cancellation and revoke-only channel-reuse cases passed 10 additional repetitions each. The final reviewer-found revoke lifecycle gap began RED with `Native messaging transport already has an active request`; acknowledged revoke now settles all outstanding old-binding exchanges through typed nonfatal cancellation, and the same port resumes, inspects, and shuts down with zero clicks/disconnects. Focused plus Task 4 compatibility passed 135/135; full `npm test` passed 790 / failed 0 / skipped 2; `npm run verify` passed all five steps with unchanged fingerprint `881c44763963086a1b56cacea71370fa3d0253f2281fe3704e640bc51e85886a` in `data/verification/2026-09-21T14-37-29.117Z-84a84662-3bce-41fe-a656-51d5d895e49d/summary.json`; all demos reported zero real external effects. Final post-report diff check remains.
- Task 5 final round-4 resolution: remaining P2/R4 independently reproduced RED against `0b083b4`: compiled content applied cancellation, its acknowledgement was held, and rejected commit delivery reached background first, disconnecting the native port despite zero mutation. Background now retains/awaits the exact validated cancellation-completion promise before classifying the concurrent commit reply. Confirmed cancellation alone supersedes the old completion; already-settled mutation retains its result; failed/malformed acknowledgement stays fatal; missing acknowledgement retains bounded native timeout. No protocol, expiry, content-authority, or permissions change.
- Task 5 final round-4 TDD: five full framed/background/compiled-content/synthetic-provider ordering cases began RED and passed after the narrow fix. Confirmed cancellation proves zero mutation/disconnect, exactly one settlement/reply, and same-port inspect/handoff/resume/inspect/shutdown. Failure/malformed/timeout cases prove fail-closed behavior and harmless late completion. Inverse successful mutation proves exactly one booking and no false cancellation. Nine timing-sensitive regressions repeated ten times passed 90/90.
- Task 5 final round-4 evidence: focused plus compatibility 140/140; full `npm test` 795 passed / 0 failed / 2 environment-dependent skips; `npm run verify` passed all five steps with unchanged fingerprint `7fc91441ed7464aef2d0d245ce831213d636167ed7dcc62ddc611a172a6dd703` in `data/verification/2026-09-21T14-50-07.134Z-a189a768-c814-4715-8852-ead760bf892d/summary.json`. Every demo reported zero real external effects. Final post-report `git diff --check` passed before the scoped `fix: coordinate browser cancellation acknowledgements` commit; no hosted CI or real-provider claim.
- Task 6: implementation complete on Task 5 base `66f2449`; delivered for the scoped `feat: protect private browser connections` commit.
- Task 6 delivered: exact callback-only `SecretProvider`, synthetic CI storage, a bounded fail-closed macOS native-helper transport contract, purpose-bound native-host access, private-profile custody and ordered disconnect, and authenticated metadata-only control/API/UI projection.
- Task 6 boundary: no real credential, Keychain, signed helper, browser installation, portal, network, or Task 7 visa-adapter behavior was used or added. Live-mode FDE is an explicit owner acknowledgment, not OS attestation.
- Task 6 TDD: the initial four-suite RED failed 4/4 on missing exports. Later RED/GREEN slices covered exact scoping and errors, buffer zeroing, helper replacement/malformed output/timeout/cancellation, process channels, profile custody/ownership, disconnect order and selected deletion, control sanitization/UI, and runtime-random canary leakage. Self-review REDs closed stalled identity timeout, unexpected non-read secret output, branded cloud path, recreated-path ownership, and unknown deletion-purpose gaps.
- Task 6 canary evidence: runtime-random username/password/security-answer values were scanned in raw, hex, and base64url form against journal, jobs, connection/control, browser/model surfaces, errors, argv/environment, a real encrypted SQLite backup, and generated verification/screenshot/trace artifacts; none escaped. A separate helper capture proved the value existed only in framed stdin and not argv/environment/stdout.
- Task 6 evidence: focused contract/leakage gate 13/13; full `npm test` 810 passed / 0 failed / 2 environment-dependent skips; `npm run verify` passed all five steps with unchanged fingerprint `fe71e0442de6559bd679b7a7cf8fcd56c98046abc8996c7547d21c5e9948d969` in `data/verification/2026-09-21T15-25-23.091Z-1024e312-b01a-4a88-acb5-f1a70e106250/summary.json`; all demos reported zero real external effects. Final post-report `git diff --check` pending before commit.
- Task 6 independent-review resolution: verified all nine findings in `task-6-review.md`; all were valid. Default local composition now binds exact durable generation revocation, browser shutdown, native broker revocation, and durable grant/monitor/job terminalization. A private flushed interprocess profile-custody journal with pinned identity and explicit PID-death recovery replaces process-local ownership. Phase-specific cleanup, exact absence reconciliation, completion receipts, same-request sharing, and restart replay make disconnect retryable without restoring authority or recursive deletion.
- Task 6 helper/control resolution: real early-exit subprocesses no longer surface uncaught stdin `EPIPE`; final-identity and queued-late cancellation are fenced before callback entry; owned returned/chunk/copy buffers are zeroed on rejection paths; Keychain lists enforce bounded exact request scope and uniqueness. CloudDocs/Mobile Documents/Box Sync forms fail closed, and destructive choices have visible associated labels, default unchecked state, exact selection, and explicit failed-cleanup retry.
- Task 6 review TDD/evidence: P1 regressions began RED for no-op composition and same-process-only custody; further REDs covered retry/restart phases, helper early exit, final-identity cancellation, retained buffers, foreign list metadata, canonical cloud paths, UI labels, completed-journal recovery, and duplicate checkpoints. Final focused/control/browser/monitor gate passed 136/136; full `npm test` passed 827 / failed 0 / skipped 2; `npm run verify` passed all five steps with unchanged fingerprint `6918cc017307f29b0695f1a48b455c705e2a93d66584e45ab775cb84f8da23b0` in `data/verification/2026-09-21T16-07-05.998Z-b3e060aa-4d33-41aa-a986-e87e49c13860/summary.json`; every demo reported zero real external effects. Runtime-random canaries were absent in raw/hex/base64url scans of the complete temporary tree and actual verification artifacts; final post-report `git diff --check` passed before the scoped review-fix commit.
- Task 6 round-2 review resolution: verified and reproduced all four remaining findings before production edits. Stale custody recovery now serializes through one private claim, compares pinned custody-directory/state inode identities plus exact source digest immediately before atomic owner replacement, and never opens a custody-directory gap. All Chrome Singleton names are rejected by `lstat` even when dangling. Disconnect advances authoritative progress only after a flushed atomic checkpoint and cannot succeed before an exact durable receipt plus custody release. Native helper failures zero both stream aggregates and all retained chunks regardless of completion order.
- Task 6 round-2 TDD/evidence: REDs proved two held subprocesses could both recover, dangling Singleton markers passed, a failed checkpoint advanced in-memory progress, swallowed release returned success, and completed fd 3 bytes survived later metadata overflow. Ten repeated real-subprocess runs exercised 30/30 races with exactly one winner. Thirteen in-process persistence faults and nine restart recoveries cover every cleanup checkpoint, secret deletion, completion, receipt, release, and partial/post-write final failures. The expanded focused gate passed 143 / failed 0 / skipped 1 socket fixture; full `npm test` passed 834 / failed 0 / skipped 3; `npm run verify` passed all five steps with unchanged fingerprint `777bda1c91fba2b45100abc324e6bfe56c721d7826b093a96fe9428b5380c43d` in `data/verification/2026-09-21T16-41-23.327Z-4588dd8e-cf31-46de-8853-3d96a2c9cb05/summary.json`. Post-verification runtime-canary tests passed 2/2; demos reported zero real external effects. The final diff check passed immediately before the scoped round-2 commit.
- Task 6 round-3 final-review resolution: verified and reproduced both remaining P2 findings. Recovery claims now atomically publish claimant PID/ID plus custody, registration, and exact source directory/state/digest identities; deterministic hard-link election generations allow one live claimant and safely supersede only dead generations without a custody-directory gap. Exact receipt/custody reconciliation repairs checkpoint-stage, receipt-link, owner-unlink, custody-rmdir, and parent-fsync boundaries using bounded private identity-matching artifacts only; unknown artifacts fail closed and profile contents are never recursively removed.
- Task 6 round-3 TDD/evidence: initial REDs left zero recovery winners, rejected checkpoint-stage restart, failed same-process release twice, and ignored a foreign staging artifact. Ten stress runs passed 80/80 recovery crash/concurrency cases, 40/40 disconnect killpoints, 10/10 same-process release-fsync retries, and 30/30 prior held-snapshot races. A stress-only partial-claim/disappearing-loser race was reproduced before atomic claim staging and `ENOENT`-only cleanup reconciliation made all repetitions green. Final focused gate passed 147 / failed 0 / skipped 1 socket fixture; full `npm test` passed 838 / failed 0 / skipped 3; `npm run verify` passed all five steps with unchanged fingerprint `a4de6aea35e1766730ed9cd521fc790fae0eeeb3c9b3a939eede20df00b500db` in `data/verification/2026-09-21T17-23-49.691Z-aac13383-940e-42af-b160-488d7c4beeb5/summary.json`; post-verification runtime-canary tests passed 2/2 and demos reported zero real external effects. The final diff check passed immediately before the scoped round-3 commit.
- Task 6 round-4 review resolution: verified sole P2/R3.1 and reproduced seven abrupt partial checkpoint/receipt failures, three partial recovery-owner staging failures, and a legacy complete-header/partial-event failure before their scoped fixes. Ordinary stages now have an exact custody-bound filename independent of their bytes; recovery stages require an exact published source-bound claim; legacy random stages require a complete matching ownership header. Only an elected dead-owner replacement removes validated stages, with canonical inode/digest and election rechecks and no early removal of the election chain. An incomplete fixed receipt requires the exclusive completed canonical owner and exact expected-byte prefix. Canonical journals/receipts alone remain authority; unknown files, mismatched identities/provenance, symlinks, hardlinks, unsafe modes and live writers/claimants fail closed. No recursive profile deletion or Task 7 behavior was introduced.
- Task 6 round-4 stress evidence: ten runs passed 200 selected test invocations, including 80 ordinary partial-stage exits, 80 interrupted recovery-stage writes followed by 40 new one-winner races, 80 prior crashed-claim races, 30 held-snapshot races, 40 complete-write finalization boundaries and 10 release-fsync retries. New stages cover zero bytes, partial headers, complete headers, and header-plus-partial-body/event writes; repeated restarts preserve only committed canonical progress. Headerless ambiguous legacy random stages intentionally remain rejected.
- Task 6 round-4 final evidence: focused gate 190 tests / 187 passed / 0 failed / 3 skips; full `npm test` and final verification full-suite gate each recorded 883 tests / 878 passed / 0 failed / 5 skips. The two new foreign-UID fixtures explicitly skip on filesystem `chown` `EINVAL`; the existing Unix-socket, report-owner-UID and Windows-only fixtures retain their environmental skips. `npm run verify` passed all five steps with unchanged fingerprint `9a9e811861a74bb27ad1bcac7cc7288a61efedbe4c28768a9cb06abdb309cb4d` in `data/verification/2026-09-21T17-50-35.945Z-b5309791-7536-4e03-bb17-49cbaccdd80c/summary.json`; all demos reported zero real external effects. Post-verification runtime-canary tests passed 2/2, including scans of actual new verification artifacts. The final executable tree was verified on dirty base `966286c`; only evidence/report/ledger edits followed. Final post-report diff check is performed immediately before the scoped commit. No hosted CI, real Keychain, portal, credential or live acceptance claim.
- Task 7: disabled China visa adapter discovery is complete on Task 6 base `47a8f3c`; it adds the exact one-effect Beijing policy, twelve versioned page-state contracts, state-specific sanitized supervised discovery, authenticated/tamper-evident fixture validation, fenced synthetic execution, exact confirmation/readback, unknown verification-only recovery, disabled registry/native-host defaults, and sanitized control API/UI readiness.
- Task 7 boundary: no live adapter registration, portal call, credential, browser installation, polling, account access, or booking occurred. Even a valid local fixture remains `liveRegistration: disabled`; owner-laptop discovery, terms/origin/roster/polling review, private connection, separate active grant, installation, and independent review remain future gates.
- Task 7 TDD/evidence: baseline 883 tests / 878 passed / 0 failed / 5 skips; initial four-suite import RED and UI/API RED were followed by focused GREEN. Review RED/GREEN slices closed typed readback, exact booking fields, exact reserved-grant/action/expected-result binding, state-specific discovery, selector-presence failure, complete report validation, and fixture-tamper gaps. Final adapter suites passed 15/15; focused plus compatibility passed 118/118; final full suite passed 899 / failed 0 / skipped 5.
- Task 7 verification: `npm run verify` passed all five steps, recorded 899 tests / 894 passed / 0 failed / 5 environment-dependent skips, and all demos reported zero real external effects. Fingerprint `89b079e1187f342f196da8120ebd367eea46aa4b9e197f92d55585c530a08d8a` was unchanged in `data/verification/2026-09-21T18-52-04.253Z-4a0f816a-ddab-4047-b7a0-67c652bb12b6/summary.json`. Final post-report diff check remains before the scoped commit; no hosted CI or live acceptance claim.
- Task 7 review round 1: independently reproduced all five findings against `d4b6999` before editing. The synthetic-only production composition now consumes the exact durable running reservation, grant, connection, installation, service claim and Task 5 `BrowserSession`; journals encrypted exact intent before mutation; finalizes through the service runtime; and startup recovery converts abrupt before-intent, after-intent, after-mutation and after-confirmation interruptions to durable unknown/verification-only with no second submit. Strict review binds candidate/date/time/Beijing/timezone/full roster/identity/terms/absence/booking type with no extra fields. Confirmation and complete appointment readback are distinct and retain one reference. Every post-submit throw becomes fixed unknown. Discovery fixtures now use a secret-key HMAC outside the artifact with owner/installation/session/freshness bindings; rehashing, wrong key/session and expiry fail.
- Task 7 round-1 TDD/evidence: all five independent probes began RED. The real encrypted-store/service-runtime/BrowserSession suite passes 8/8, including four abrupt reopen phases, encrypted intent plus confirmation evidence, immutable confirmation-reference recovery, and verification-only reconciliation; focused adapter/browser/monitor/storage/runtime compatibility passes 261/261. Full `npm test` records 911 tests / 906 passed / 0 failed / 5 environment-dependent skips. Final `npm run verify` passed all five steps with unchanged fingerprint `1a7c9a50ec75b747dd593afd470a8d60731bb8cec33e9aeb9736bb57208af6d9` in `data/verification/2026-09-21T19-58-00.794Z-bac72015-18f9-46c5-96b0-e5e8a0a94e0c/summary.json`; all demos reported zero real external effects. Live registration/native-host defaults remain disabled; no portal, credential, browser install, account, polling or external effect was used.
- Task 7 round-2 review resolution: verified all five re-review findings against `7139423` and accepted them. Exact durable action/attempt/grant/connection/profile/installation authority is re-read around every continuation; compiled content derives the safety fields from exact DOM attributes; recovery recognizes and binds the current readback-capable state; journaled intent crosses the typed browser/content/loopback portal path before selection; and non-cooperative execute/readback is bounded by the trusted runtime race with late settlement consumed and fenced.
- Task 7 round-2 TDD/recovery evidence: inherited RED/GREEN records cover expiry and in-flight revocation plus contradictory compiled DOM evidence. Additional exact-state, intent-bridge, and never/late-settling attacks are GREEN; late execute/readback resolve and reject append no journal entry and cause no submit or portal mutation. Abrupt after-mutation/confirmation fixtures now use the typed intent handshake. An attempted BrowserSession-level early return failed five existing native cancellation-order tests and was reverted, preserving validated no-click acknowledgement while the outer runtime still releases its worker.
- Task 7 round-2 final evidence: exact reviewer gate 296/296; final full `npm test` 921 total / 916 passed / 0 failed / 5 environment-dependent skips; `npm run verify` passed all five gates with unchanged fingerprint `349a69404b9be396770ef292d40d313593ef2e8dfac2d4f891fb30d9b86c5b6c` in `data/verification/2026-09-21T21-31-39.354Z-5ba9c2f0-26bc-47e5-9659-63284fd64dc7/summary.json`. All demos reported zero real external effects. Live registration remains disabled; no hosted CI or real-provider acceptance is claimed.
