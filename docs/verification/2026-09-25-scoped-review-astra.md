# Scoped final recovery re-review — Astra

Reviewed production/test fix range `dc7eeee9512a948bddd73203c3d8da9717a1abb4..388afc4a901e56c413ae6d88b5b692f0512646d8` in `/workspace/scratch/871e778ff34c/behalvo-published/.worktrees/monitored-actions`. Observed HEAD `e815410`; its only subsequent change is the documentation checkpoint. Tracked worktree was clean. This is the one scoped re-review of the completed broad review, not a new broad branch review.

Read AGENTS, repository development/release/recovery instructions, prior Sol/Astra final reports, failed-verification diagnosis, relevant monitored-actions specification, Task 8C lifecycle/recovery design, fix report, changed source/tests and adjacent call paths. Inspected complete fix diff and ran `git diff --check dc7eeee..388afc4` successfully. No source changes, test execution, build, external calls, live accounts, push, or merge.

## Finding dispositions

| Finding | Disposition | Evidence |
| --- | --- | --- |
| S1/A1: pre-evaluation reservation check bypasses durable drift revocation | **ADDRESSED** | `src/monitoring/service.ts:367` now invokes the lifecycle-only predicate. `src/monitoring/policy.ts:144-157` checks the planned monitor exists, is active, and has no in-flight job before evaluation; complete plan validation remains in the subsequent reservation-ready predicate. Connection drift consequently reaches `evaluate()` and its durable `material_drift` revocation. New revoked/regenerated/rebound cases in `tests/monitoring-service.test.mjs:1219-1238` assert revoked grant, stopped monitor, zero actions/selection and exact replay. Existing paused/queued/running/stopped boundary cases remain immediately adjacent. |
| A2: lost activation acknowledgement erases the exact retirement target | **ADDRESSED** | `src/browser/native-host.ts:264-268` retains the activating binding as `uncertain` on failure. `revoke()` cannot treat it as an absent-binding success: its active-binding check fails, so `BrowserSession.resume()` invokes `candidateRetirementFailed` and durable resume handling preserves the candidate as a failed handoff. The new failed-transport operation guard also prevents reuse. The framed compiled-background/content regression in `tests/monitoring-recovery.test.mjs` deliberately applies activation while dropping its ACK, verifies the exact durable candidate, reopens storage, proves effect-free reconstruction/replay and blocked plain resume, then verifies explicit exact revocation before a distinct fresh activation. |
| A3: interrupted resume discards its persisted candidate | **ADDRESSED** | `src/storage/sqlite-store.ts:1799-1810` appends candidate retirement-failure and interrupted-resume result in the existing recovery transaction before clearing resume. The reducer rejects an interrupted result without an exact failed handoff match. `src/service/synthetic-monitoring.ts:97-115` restores a persisted candidate as the fault target because composition precedes startup repair. The crash-snapshot regression activates the candidate, reopens storage, reconstructs composition before repair, checks failed handoff/paused monitor/interrupted job and zero browser I/O, then proves explicit retirement precedes fresh activation. A direct reducer regression rejects candidate erasure. No-candidate recovery still appends only the interrupted result; it invents no retirement target. |
| Exact repeated retirement ACK/control replay test repair | **ADDRESSED** | `tests/browser-protocol.test.mjs:316-322` now checks exact content ACK with the observed document ID and exact background ACK using separate fresh control IDs. It separately rejects reused controls at both boundaries and retains stale-commit/zero-click assertions. No production protocol relaxation was introduced. |
| Deterministic actual transport-entry deadline test repair | **ADDRESSED** | Both gesture and readback late resolve/reject loops in `tests/us-visa-runtime.test.mjs:539-603` enable test-owned Date/setTimeout, await an entry signal raced against premature drain failure, then advance the owned deadline. A real test timeout bounds hangs. The existing unknown/blocked, released-worker, no-mutation and no-late-journal-write assertions remain. These tests now exercise entered non-cooperative transport operations rather than potentially rejecting unused gates. |

## Verdict and evidence limits

**Scoped SPEC: PASS. Scoped QUALITY: PASS.** All three prior production findings and both diagnosed test repairs are addressed. No supported new regression was found within this fix diff. No further source change is requested by this review.

Inspected the targeted recovery log's terminal **11 passed, 0 failed, 0 skipped** summary and the earlier preserved focused log's terminal **227 passed, 0 failed, 0 skipped** summary. The fresh recovery focused log currently contains passing case lines but ends without its terminal summary; its fresh 227/227 result and exit are supplied by the controller/fix report, not independently established by that truncated raw file. This review did not rerun either gate. The controller subsequently supplied the completed final exact-head `npm run verify`. I inspected `data/verification/2026-09-25T06-17-32.790Z-30bdcfa9-10ec-47b0-a79a-fefaebb57eb0/summary.json`: terminal status passed, all six gates passed with exit 0, unchanged exact HEAD `e8154101247a8dfd500ccd75fbf68670da50e18d`, identical source fingerprints, and clean tree before/after. The raw check stdout terminal totals are 1,053 tests, 1,048 passed, 0 failed, 5 skipped. This is fresh controller-run full verification evidence; I did not rerun it. The original failed verification remains historical evidence. Neither review nor passing verification authorizes publication/merge.

Acceptance remains synthetic/offline. Installed Chrome/native host, signed Keychain helper, real accounts/current portal semantics, owner-laptop discovery, live activation, real booking and hosted CI were not assessed or performed.

Actual usage: independent scoped authority/recovery reviewer, assigned `gpt-6-astra`, high effort; zero implementation retries, zero subagents, zero synthetic probes/test executions, one read-only diff-format check. Result: all five scoped items addressed, no new finding; controller-run final full verification inspected and passing.
